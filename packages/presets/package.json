{
  "name": "@cv/presets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Built-in CV design presets: Klassisch, Modern, Minimal.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@cv/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "latest",
    "vitest": "latest"
  }
}

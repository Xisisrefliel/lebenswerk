{
  "name": "@cv/renderer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Framework-agnostic PDF/print rendering pipeline: Paged.js + window.print().",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@cv/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "latest",
    "vitest": "latest"
  }
}

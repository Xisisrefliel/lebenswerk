import { TokenEditor } from './TokenEditor.js';

/**
 * Design settings panel: colors, fonts, spacing, global options.
 * Shown in the settings tab of the editor.
 */
export function DesignerPanel() {
  return <TokenEditor />;
}
